pipeline {
    agent any { label 'slave1' }

    stages {
        stage('Checkout') {
            steps {
                // Jenkins already checks out repo automatically
                echo 'SCM checkout done'
            }
        }

    }
}
